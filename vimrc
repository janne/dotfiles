" Basic setup
set nocompatible
filetype plugin indent on

" Don't break words in middle
set linebreak

" Show ruler
set ruler

" Search
set incsearch

" Syntax highlight
syntax on

" Fix tabs
set tabstop=2
set shiftwidth=2
set expandtab

" Maps
map <F6> :NERDTreeToggle<CR>
